<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Checked in rather than left to `@bacons/apple-targets` (which writes this exact plist on
  the first prebuild if it is missing): the plugin *reads* `NSExtension.NSExtensionPointIdentifier`
  back on later runs to recognise the target — that plus WATCHOS_DEPLOYMENT_TARGET is how it
  tells a watch complication apart from the iPhone widget in `targets/live-activity`, which
  uses the same extension point. Having it in git means the recognition never depends on a
  file the previous prebuild happened to leave behind.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>Dreaming</string>
	<key>CFBundleLocalizations</key>
	<array>
		<string>en</string>
		<string>th</string>
	</array>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>
