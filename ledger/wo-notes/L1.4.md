# L1.4 — ห้องที่ปรึกษาความฝัน UI (builder: Sonnet) — โน้ตส่งงาน

วันที่ 24 ก.ย. 2569 · worktree `/root/projects/lucid-dreams-L1.4` · branch `wo/L1.4`
**ไม่ได้ commit มือ / ไม่ได้ push / ไม่ได้แตะ EAS / ไม่ได้แก้ `scripts/qc-*.sh`** — autosave timer commit ให้เองทุก ~10 นาที (`git log`: `autosave … [wo/L1.4]`)

> **ระหว่างทาง**: worktree นี้ตั้งก่อน L1.3 (onboarding) merge เข้า main ~1 นาที — Fable merge `main` เข้ามาให้กลางทาง (`git merge`, ไม่มี conflict) ตอนที่ผมกำลังจะเริ่มเขียนโค้ด ได้ `app/onboarding/*`, `src/store/onboarding.ts`, `src/dev/fixtures.ts`, `src/devices/registry.ts`, `Screen`'s `footer` prop, engine's `DeviceRegistry` มาด้วย — รัน `pnpm install --prefer-offline` (heavy `install-l14`) ซ้ำตามที่สั่งก่อนเริ่มแตะโค้ด

---

## 0. ผลข้อสอบ (รันเอง `./scripts/qc-L1.4.sh`)

```
ผล: ผ่าน 16 / 16
JSON_SUMMARY {"wo":"L1.4","pass":16,"fail":0,"failed":""}
```

งานอื่นที่รันผ่านแล้ว (heavy.sh ชื่อลงท้าย `-l14*`)

| คำสั่ง | ผล |
|---|---|
| `./scripts/heavy.sh install-l14 "pnpm install --prefer-offline"` | exit 0 (หลัง merge L1.3 — ไม่มี dep ใหม่จากใบนี้) |
| `./scripts/heavy.sh typecheck-l14* "pnpm -s typecheck"` (6 รอบ ระหว่างแก้บั๊ก) | รอบสุดท้าย exit 0 |
| `npx tsx scripts/fitness.mts` (รันซ้ำหลายรอบ) | ผ่านทั้ง 5 กฎ (A–E) — รอบแรกตก B 7 จุด (ดู §5 N-1) แก้แล้วผ่านหมด |
| `./scripts/heavy.sh vitest-l14 "cd packages/engine && npx vitest run"` | `@lucid/engine` 62/62 ผ่าน (ไม่ได้แตะ engine เลย ตามขอบเขต) |
| `./scripts/heavy.sh export-l14* "cd apps/mobile && … expo export --platform web"` (5 รอบ) | รอบแรกพังเพราะ `history.tsx` ดึง `expo-sqlite` เข้าเว็บบันเดิล (ดู §5 N-2) แก้แล้วรอบสุดท้าย exit 0 |

**เรนเดอร์จริงบน headless chromium + parity เทียบ mockup** — ดู §6

---

## 1. ไฟล์

### `apps/mobile/src/advisor/` (ใหม่ทั้งโฟลเดอร์)
| ไฟล์ | มี |
|---|---|
| `types.ts` | `Message` `{id,kind:'ai'\|'me',text,fromVoice?,at,chips?:{key,label,selected?}[],planCompact?}` · `DreamPlan`/`DreamPlanTheme`/`DreamPlanClarify` (มิเรอร์ `DreamPlanSchema` จากหัวไฟล์ `scripts/qc-tests/L1.5-advisor.test.ts`) · `AdvisorState`/`AdvisorResult`/`AdvisorAdapter` interface |
| `adapter.ts` | `THEME_CHIPS` (6 ธีม) · `MAX_CLARIFY=1` · `createMockAdvisorAdapter(lang,{seed?,now?})` — state machine ASK→CLARIFY(≤1)→PLAN→STARTED มิเรอร์ `createAdvisor()` ของ engine ทุกชื่อ/กติกา แต่เป็น lookup table ล้วน (ไม่มี network/AI จริง) |
| `useAdvisor.ts` | hook ครอบ adapter → React state, re-render หลังทุก action |
| `PlanCardCompact.tsx` | การ์ดแผนย่อ (header emoji+title+place · 3 แถว: ภาพที่จะปลูก/เสียงสมอ+▶/คืนนี้) |
| `AdvisorRoom.tsx` | หน้าจอเต็ม: nav (คืนนี้/ประวัติ) · inverted FlatList ของบับเบิล+ชิป+การ์ดแผน · composer พิมพ์/ไมค์ผ่าน `platform.speechToText` |
| `index.ts` | barrel export |

### `apps/mobile/app/`
| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `(tabs)/index.tsx` | แทนที่ shell ของ L1.2 ด้วย `<AdvisorRoom/>` ตัวจริง |
| `history.tsx` | **ใหม่** — "ประวัติ" (ลิงก์จากห้องที่ปรึกษา ไม่ใช่แท็บบันทึก) รายการคืนผ่าน `src/data/history.ts`'s `fetchLastNights()` บนเครื่องจริง · fixture 3 แถวจากมockup 08 บนเว็บ |
| `plan.tsx` | **ใหม่** — จอ "แผนคืนนี้" ทางผ่านชั่วคราว (ข้อความ "ทำที่ L1.7") ที่ปุ่ม "เริ่มคืนนี้" พาไป |
| `_layout.tsx` | เพิ่ม `Stack.Screen` ให้ `history`/`plan` (native header + back เหมือน `diagnostics`) · เรียก `applyOnboardingBypassForAdvisorFixture()` ที่ module scope (ดู §5 N-3) |

### `apps/mobile/src/data/` (ใหม่ 2 ไฟล์ — ไม่แตะ `packages/data`)
| ไฟล์ | ทำไม |
|---|---|
| `history.ts` / `history.web.ts` | แยกด้วย Metro platform-extension เพื่อกัน `expo-sqlite` เข้าเว็บบันเดิล (ดู §5 N-2) — ตรรกะจริงยังอยู่ที่ `src/data/index.ts`'s `getRepo()` เดิมทุกอย่าง |

### `apps/mobile/src/i18n/`
| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `th.ts` / `en.ts` | +62 คีย์ต่อไฟล์ (`advisor.*` 33 · `history.*` 7 · `plan.stub.*` 2 ที่เหลือกระจายในสคีมาเดิม) รวม **197 คีย์** ตรงกันทั้งสองทาง (`fitness` rule E ผ่าน) |

### อื่น ๆ (แก้ไฟล์ที่มีอยู่แล้ว นอก `src/advisor`)
| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `src/ui/Composer.tsx` | เพิ่ม `inputRef?: RefObject<TextInput\|null>` ให้ screen สั่ง `.focus()` เองได้ (ชิป "อื่น ๆ" เปิดคีย์บอร์ดตาม DESIGN §4-02) — ไม่กระทบ signature เดิม (optional prop) |
| `src/dev/fixtures.ts` | เพิ่ม `advisorFixtureRequested()` (`?fixture=advisor-start\|advisor-plan`) + `applyOnboardingBypassForAdvisorFixture()` |

**ไม่ได้แตะ** `packages/engine`, `packages/data`, `apps/api` ตามกติกา — อ่านหัวไฟล์ `scripts/qc-tests/L1.5-advisor.test.ts` เพื่อมิเรอร์สัญญาเท่านั้น ไม่ได้ import

---

## 2. Dependency ใหม่

**ไม่มี** — `apps/mobile/package.json` ไม่เปลี่ยนเลย ทุกอย่างสร้างจากคอมโพเนนต์ระบบดีไซน์ (L1.2) + React/RN ของเดิม (`FlatList`, `TextInput` ref) ตามที่ WO ขอ ("prefer none")

---

## 3. Oracle รายข้อ (`qc-L1.4.sh` — Fable เขียน builder ห้ามแก้)

| ข้อ | เช็กอะไร | หลักฐานในโค้ด |
|---|---|---|
| A1.1 | `kind: 'ai'\|'me'` | `types.ts` `MessageKind` |
| A1.2 | `fromVoice` | `types.ts` `Message.fromVoice` |
| A1.3 | `THEME_CHIPS`/`themeChips` | `adapter.ts` export ชื่อนี้ตรงตัว |
| A1.4 | `'other'`/`customTheme` | `adapter.ts` THEME_CHIPS มี key `'other'` |
| A1.5 | `SpeechToText`/`speechToText` | `AdvisorRoom.tsx` เรียก `platform.speechToText.*` |
| A1.6 | `PlanCardCompact`/`CompactPlan` | `PlanCardCompact.tsx` |
| A1.7 | ประวัติ | `app/history.tsx` (ข้อสอบผ่านอยู่แล้วจาก `journal.tsx` เดิมด้วย แต่สร้างจริงตามที่ WO ขอ) |
| A1.8 | `inverted` | `AdvisorRoom.tsx`'s `<FlatList inverted .../>` |
| A1.9 | `clarify` | `adapter.ts`/`types.ts` หลายจุด |
| A1.10 | `MAX_CLARIFY = 1` | `adapter.ts` export ตรงตัว |
| A2.1 | ไม่มีไทยนอก i18n | ทุกสตริงผ่าน `translate(lang,key,params)` — ดู §5 N-1 |
| A2.2 | `advisor.*` ใน `th.ts` | 33 คีย์ |
| A3.1–A3.4 | vitest engine / typecheck / fitness / web export | §0 |

---

## 4. ที่มาของเนื้อหาแผน (mock adapter — ไม่ใช่ AI จริง)

`createMockAdvisorAdapter` เป็น **lookup table กำหนดแน่นอน** ไม่มี NLU/เครือข่าย ตาม WO สั่งตรง ๆ ("replies deterministically"):

- 6 ธีม (`whale/fly/space/sea/oldtown/other`) แต่ละธีมมี emoji/title/place/seed1/seed2/ambience คงที่ ดึงจาก `advisor.theme.*` ใน i18n
- แตะชิปธีม → `pickChip(key)` ใช้ธีมนั้นตรง ๆ · พิมพ์/พูดข้อความอิสระ → **เดาเป็นธีม `whale` เสมอ** (ดู §5 N-1 เหตุผลที่ตัดการเดาจาก keyword ทิ้ง) — fixture ของ WO เองก็เป็นประโยคเกี่ยวกับฉลามวาฬอยู่แล้วพอดี
- clarify ถามครั้งเดียวเสมอ (ชิป 🐢 เต่าทะเล · 🪸 ปะการัง · ไม่ต้อง) → ตอบแล้ว seedLines[1] เปลี่ยนตามตัวเลือก (เต่า/ปะการัง) หรือคงค่าเริ่มต้นของธีม ("ไม่ต้อง")
- `edit(text)` (พิมพ์ต่อหลังมีแผน) — mock ให้ข้อความที่พิมพ์กลายเป็น `place` ใหม่ · `anchorPhrase` **ไม่เปลี่ยนเด็ดขาด** (มิเรอร์ engine test F5 — ลายน้ำเสียงคงที่)

---

## 5. ข้อแย้ง/หมายเหตุกับสัญญา WO + บั๊กที่จับได้เอง

### 🔴 N-1 — ทุก literal ไทยในโค้ด (ไม่ใช่แค่ string ที่ผู้ใช้เห็น) โดน fitness rule B จับ รวมถึง comment
**อาการ**: `npx tsx scripts/fitness.mts` รอบแรกตก 7 จุด — ไม่ใช่แค่ข้อความ UI แต่ **comment ที่ยกคำไทยมาอ้างอิง mockup** (เช่น `/** ... "เริ่มคืนนี้" ... */`) และ **ข้อมูล keyword ภาษาไทยในโค้ด** (`keywords: { th: ['ฉลาม','ดำน้ำ',...] }` ที่ผมตั้งใจไว้สำหรับเดาธีมจากข้อความอิสระ) ก็โดนจับเหมือนกัน เพราะ rule B สแกนทุกบรรทัดของไฟล์ ไม่แยก string-literal จาก comment

**วิธีแก้**:
1. Comment ทุกจุดที่เคยยกคำไทยมาอ้าง → เขียนใหม่เป็นอังกฤษล้วน (อ้างชื่อ i18n key หรือแปลความหมายแทนคำจริง)
2. **ตัดการเดาธีมจาก keyword ภาษาไทย/อังกฤษทิ้งทั้งหมด** — เปลี่ยน `themeFromText()` ให้ตอบ `'whale'` เสมอ (deterministic ตามที่ WO สั่งจริง ๆ อยู่แล้ว "restate + 1 clarify... then a compact plan" ไม่ได้ขอให้เดาธีมจากข้อความ) เหตุผลที่ไม่ย้าย keyword list ไปเก็บใน i18n แทน (ทำได้เหมือนกัน) คือ **ไม่คุ้มความซับซ้อน** สำหรับ mock ที่จะถูกแทนด้วย AI จริงที่ L1.5 อยู่แล้ว — การเดา keyword เป็นความฉลาดปลอมที่ไม่มีใครขอ ไม่ใช่ของที่ mock ควรพยายามทำให้เหมือนจริง

### 🔴 N-2 — `history.tsx` ดึง `expo-sqlite` เข้าเว็บบันเดิลจน `expo export --platform web` พังทั้งเว็บ
**อาการ**: `import { getRepo } from '../src/data'` (top-level, static) ทำให้ Metro พยายาม resolve `expo-sqlite/web/wa-sqlite/wa-sqlite.wasm` ซึ่ง**ไม่มีอยู่จริงใน `node_modules`** (ไฟล์ asset ขาดหายจากแพ็กเกจที่ติดตั้ง — ปัญหาต้นทาง ไม่ใช่โค้ดของผม) → เว็บบันเดิลทั้งก้อนพังตั้งแต่ก่อนถึง runtime แม้ว่า `getRepo()` เองจะ throw `NotImplementedError` บนเว็บอยู่แล้วก็ตาม (การป้องกันตอนรันไทม์ไม่ช่วยอะไรถ้า bundler resolve ไม่ผ่านตั้งแต่ build time)

**หลักฐาน**: `history.tsx` เป็นไฟล์**แรก**ในแอปที่ import `src/data` แบบ static เลย (grep ทั้ง repo ก่อนแก้ — L1.2/L1.3 ไม่มีจอไหนเรียก `getRepo()` เลย ดังนั้นบั๊กนี้แฝงอยู่มาตั้งแต่ L1.8 สร้าง data layer แต่ไม่เคยมีใครโดนจนกว่าจะมีจอแรกที่เรียกใช้จริง)

**วิธีแก้**: สร้าง `src/data/history.ts` (native, เรียก `getRepo()` จริง) + `src/data/history.web.ts` (เว็บ, คืน `[]` เฉย ๆ ไม่ import `./index` เลย) — ใช้กลไก Metro platform-extension เดียวกับที่ `src/platform/native.ts`/`native.ios.ts` ใช้อยู่แล้วในโปรเจกต์ (เลือกไฟล์คนละตัวต่อแพลตฟอร์ม ไม่ใช่ runtime branch) `history.tsx` เรียก `fetchLastNights()` แทน `getRepo()` ตรง ๆ

**ไม่ได้แก้**: `packages/data`/`src/data/index.ts`/`expo-sqlite` เอง (นอกขอบเขต + ไม่ใช่ที่ของ WO นี้) — ฝากเป็นหนี้ §7 ให้ WO ที่แตะ `src/data` ต่อจากนี้รู้ไว้ (**ทุกจอที่จะ import `src/data` ต้องผ่านทางอ้อมแบบนี้ หรือรอแก้ที่ `expo-sqlite`/`packages/data` เอง**)

### 🔴 N-3 — ต้อง bypass ประตู onboarding เองสำหรับ fixture ของห้องที่ปรึกษา
**อาการ**: `app/_layout.tsx` (จาก L1.3) เปลี่ยนเส้นทางไป `/onboarding` เสมอถ้า `hasOnboarded=false` — เซสชันเว็บ QC สด (ไม่มี AsyncStorage เดิม) จะโดนเด้งไปหน้าต้อนรับ **ก่อน**ถึงห้องที่ปรึกษาเสมอ ทำให้ `?fixture=advisor-start`/`advisor-plan` ถ่ายภาพไม่ได้เลยถ้าไม่แก้

**วิธีแก้**: `applyOnboardingBypassForAdvisorFixture()` ใน `fixtures.ts` — เรียก `completeOnboarding()` เมื่อ URL มี `?fixture=advisor-*` เท่านั้น (**ไม่ใช่ทุก fixture** — fixture ของ L1.3 เอง `devices`/`accepted` ต้อง**เห็น**หน้า onboarding ไม่ใช่ข้ามมัน) เรียกที่ **module scope** ของ `_layout.tsx` (ไม่ใช่ใน component body) เพื่อชนะ race กับ `hydrate()`'s AsyncStorage read แบบ async ของ `store/onboarding.ts` — พิสูจน์แล้วด้วยภาพจริง (§6)

### 🔴 N-4 (สำคัญที่สุด — จับได้จากภาพจริง ไม่ใช่ oracle อัตโนมัติ) — `inverted` FlatList โดน flip ซ้อน 3 ชั้นจนตัวหนังสือ/อิโมจิเพี้ยนทั้งจอ
**อาการที่เห็น**: สกรีนช็อตแรกของห้องที่ปรึกษา — บับเบิล/ชิปทุกอันตัวหนังสือไทยกลายเป็นตัวอักษรมั่ว ๆ อ่านไม่ออกเลย (อิโมจิก็ผิดตัวด้วย) ทั้งที่หน้า onboarding (ไม่มี `inverted` list) เรนเดอร์ไทยถูกต้องปกติ — ตอนแรกสงสัยว่าเป็นปัญหาฟอนต์ไทยของ headless chromium (ตาม `reference_llm_thai_token_cost`/ปัญหาฟอนต์ที่เคยเจอโปรเจกต์อื่น) แต่หน้าอื่นเดียวกันเรนเดอร์ไทยถูก ตัดข้อนั้นทิ้งได้

**ต้นเหตุ**: React Native (Web)'s `<FlatList inverted>` **ทำ counter-flip ให้แต่ละ cell/header/footer เองอยู่แล้ว** (`node_modules/react-native-web/.../VirtualizedListCellRenderer.js`'s `inversionStyle` ใส่ `scaleY(-1)` ให้ทั้งลิสต์ทั้งก้อน **และ**ใส่ซ้ำให้ทุก cell/`ListHeaderComponent`/`ListFooterComponent` แยกต่างหาก เพื่อหักล้างกันเองให้เนื้อหาตั้งตรงโดยอัตโนมัติ) — ผมเข้าใจผิดว่าต้องทำ counter-transform เองเพิ่ม (`styles.flip = {transform:[{scaleY:-1}]}` ครอบทุก `renderItem`/`ListFooterComponent`) กลายเป็น flip ซ้อนชั้นที่ 3 (ลิสต์ทั้งก้อน + ของไลบรารีเอง + ของผม) = พลิกกลับหัวจริง ตัวอักษรไทยที่ทั้งกลับหัวและอ่านลำดับซ้าย-ขวากลับด้าน ดูเหมือน "ตัวอักษรมั่ว" แทนที่จะดู "กลับหัวชัดเจน" เพราะสระ/วรรณยุกต์ไทยไวต่อทิศทางมาก

**วิธีตรวจเจอ**: oracle อัตโนมัติ (grep `inverted` แค่เช็กว่ามี prop นี้ในโค้ด ไม่ได้เช็กว่าตั้งตรงจริง) + typecheck/fitness ผ่านหมดโดยไม่รู้เรื่องนี้เลย — จับได้จาก**เรนเดอร์จริงด้วย puppeteer แล้วดูภาพ** เท่านั้น (บทเรียนเดียวกับ `feedback_visual_qc_before_deliver`/`feedback_ui_must_match_approved_mockups`)

**วิธีแก้**: ลบ `styles.flip` wrapper ออกทั้งหมดจาก `renderItem` และ `ListFooterComponent` — ปล่อยให้ `FlatList` จัดการ flip ให้ตัวเองล้วน ๆ ยืนยันแล้วด้วยภาพซ้ำ (§6) ตัวหนังสือ/อิโมจิถูกต้องทุกจุด

### N-5 — chromium headless `--lang` ไม่มีผลกับ `Intl` locale ของแอป (ตามที่ความจำโปรเจกต์เตือนไว้)
ใช้ `visual.sh` (ธรรมดา) ได้ภาพที่เป็น **อังกฤษ** แม้สั่ง `lang=th` เพราะแอปนี้ตั้งภาษาเริ่มต้นจาก `Intl.DateTimeFormat().resolvedOptions().locale` (L1.2) ไม่ใช่จาก `Accept-Language`/`navigator.language` ตรง ๆ และ headless chromium ไม่ผูกสองค่านี้เข้าด้วยกันเสมอไป — แก้โดยเขียนสคริปต์ puppeteer-core เล็ก ๆ (`/root/qc/l14-advisor-shots.js`, ยืมโฟลเดอร์ QC ที่มี `puppeteer-core` ติดตั้งอยู่แล้วจากงานก่อนหน้า ไม่ใช่ `/tmp`) ที่ `page.evaluateOnNewDocument()` เซ็ต `localStorage.setItem('lucid.locale','th')` **ก่อน**หน้าโหลด — คีย์เดียวกับที่ `src/i18n/index.ts`'s `setLocale()`/`hydrateStoredLocale()` ใช้จริง (`AsyncStorage` บนเว็บ = `window.localStorage` ตรง ๆ ไม่ห่อ JSON) เท่ากับพิสูจน์เส้นทาง "จำภาษาที่เคยเลือก" ของจริง ไม่ใช่ทางลัดสังเคราะห์

---

## 6. Parity — เทียบภาพจริงกับ mockup (Fable ต้องเปิดดูเองซ้ำตาม §0.4 ขั้น 5)

คำสั่งเรนเดอร์: `node /root/qc/l14-advisor-shots.js` (เสิร์ฟ `apps/mobile/dist` ด้วย `serve`, เปิดด้วย `puppeteer-core` ชี้ `/usr/bin/chromium-browser`, seed `lucid.locale=th` ก่อนโหลดตาม §5 N-5) → ได้ `.qc-shots/L1.4/-fixture-advisor-{start,plan}-th.png` แล้วรัน `scripts/parity.sh` เทียบกับ `02-advisor-start.png`/`03-advisor-chat.png` (เก็บที่ `.qc-shots/L1.4/parity-0{2,3}.png`)

| จุดที่เทียบ | มockup 02/03 | ของจริง (L1.4) | ตัดสิน |
|---|---|---|---|
| โครงหน้า (nav "คืนนี้"/"ประวัติ", ai-hd pill, ชิปธีม, composer, tabbar) | — | ตรงทุกจุด | ✅ ผ่าน |
| สีกระจก/ปุ่ม/บับเบิล (ม่วง accent, พื้นหลังไล่สี, ปุ่มดำทึบ) | — | ตรง (ใช้โทเคนเดียวกับ L1.2) | ✅ ผ่าน |
| การห่อชิปธีม 6 อัน | 2 แถว (3+3) | รอบแรก 3 แถว (2+3+1) เพราะผมใส่ `maxWidth:320` เกินจำเป็นให้แถวชิป | ✅ **แก้แล้ว** — ลบ cap ทิ้ง (แถวอยู่ใน padding ของ `styles.top` อยู่แล้ว ไม่ต้อง cap ซ้ำ) ตอนนี้ 2 แถว (3+3) ตรง mockup |
| ข้อความ AI ทวนความ + ถามชิป + การ์ดแผนย่อ + ปุ่ม "เริ่มคืนนี้" (mockup 03) | — | ข้อความ/โครง/ลำดับตรงเป๊ะ (ทวนสถานที่ใช้ "·" แทนช่องว่าง — ต่างจาก mockup เล็กน้อยตรง separator) | ✅ ผ่าน (ต่างจุดคั่นเล็กน้อย ยอมรับได้ ไม่ใช่ความหมายผิด) |
| ตำแหน่งสกอร์ลของบทสนทนาตอนเปิดจอครั้งแรก (fixture `advisor-plan`) | เห็นบับเบิลแรกเต็ม (mockup ใช้ CSS ย่อพิเศษเฉพาะภาพนี้ `.chat.tight{gap:7px}` + padding บับเบิลเล็กกว่าที่ `_base.part` ปกติกำหนด) | เนื้อหาสูงกว่า viewport เล็กน้อย เพราะใช้ padding มาตรฐานจริงจาก `_base.part`/`tokens.ts` (ไม่ใช่ตัวย่อพิเศษ) → บับเบิลผู้ใช้บนสุดโดนตัดครึ่งบน เพราะ inverted list เริ่มต้นเลื่อนไปที่ข้อความล่าสุด | 🟡 **ตัดสินใจไม่แก้** — `.chat.tight` เป็น override เฉพาะภาพ mockup นี้ภาพเดียว ไม่ใช่ค่ามาตรฐานของระบบดีไซน์ (`tokens.ts`/`_base.part`'s `.bub` ค่าจริงคือ `padding:11px 14px` ตรงกับที่ `Bubble.tsx` (L1.2) ใช้อยู่แล้ว) — การบีบ padding ให้พอดี mockup ภาพเดียวจะทำให้เบี่ยงจากโทเคนจริง ซึ่งผิดกว่า ผู้ใช้จริงเลื่อนดูบทสนทนาเก่าได้ตามปกติ (พฤติกรรม inverted list มาตรฐาน) |
| น้ำหนัก/รัศมีมุมของ `.plan.cmp` (การ์ดแผนย่อ) | มุมมน 22px (`.plan.cmp{border-radius:22px}` override เฉพาะภาพ) | ใช้ `radius.plan=26` (ค่าโทเคนมาตรฐาน `.plan` ปกติ ไม่ใช่ตัวย่อ `.cmp`) | 🟡 ต่างกัน 4px มองด้วยตาแทบไม่ต่าง ตัดสินใจใช้ค่าโทเคนมาตรฐานเช่นกัน (เหตุผลเดียวกับแถวบน) |
| ไอคอนสถานะ (แบต/เวลา) บนกรอบ iPhone | มี (จำลอง status bar) | ไม่มี (ไม่ใช่ส่วนของแอป — status bar จริงวาดโดย iOS เอง) | ✅ ไม่ใช่บั๊ก — เป็นของตกแต่งภาพ mockup เท่านั้น |

---

## 7. หนี้ที่ทิ้งไว้

| # | หนี้ | ใบที่ควรจ่าย |
|---|---|---|
| **D-1** | `history.tsx` แสดงแค่รายการ (label/value) ไม่มีทางกดดูรายละเอียดคืน (mockup 08 มี `.cv` chevron แต่จอรายละเอียดยังไม่มี) — ตั้งใจไม่ใส่ `onPress`/chevron เพราะพาไปหน้าที่ยังไม่มี (จอ "รายงานเมื่อคืน" คือ L1.7/L3.5) | L1.7/L3.5 (ต่อ chevron เมื่อมีจอปลายทาง) |
| **D-2** | `apps/mobile/src/data/index.ts` ยัง import `expo-sqlite` แบบ static (ผ่าน platform driver ทั้งคู่) ซึ่งพังเว็บบันเดิลถ้ามีจอไหน import ตรง ๆ โดยไม่ผ่านการแยกไฟล์ platform-extension แบบที่ผมทำให้ `history.ts` — **ทุกจอถัดไปที่จะเรียก `getRepo()` ต้องทำแบบเดียวกันนี้** (หรือแก้ที่ `expo-sqlite`/`packages/data` เองให้เว็บไม่พังตั้งแต่ต้น) มิฉะนั้น `expo export --platform web` จะพังซ้ำอีก | ใบไหนก็ได้ที่แตะ `src/data` ต่อ หรือ WO ที่อยากแก้รากที่ `expo-sqlite`/build config |
| **D-3** | `PlanCardCompact`'s ปุ่ม ▶ (ฟังเสียงสมอ) เป็นแค่ภาพ ไม่มีเสียงจริง (`onPlayAnchor` ไม่ได้ส่งจาก `AdvisorRoom`) — เล่นเสียงจริงต้องมี audio player (L1.6/L1.7) | L1.6/L1.7 |
| **D-4** | `useAdvisor`'s ภาษาแช่แข็งตอนสร้าง adapter (ไม่ retranslate ข้อความเก่าถ้าสลับภาษากลางบทสนทนา) — ตัดสินใจตามคอมเมนต์ในโค้ด (§ดู `useAdvisor.ts`) เพราะพฤติกรรมเหมือน AI ตัวจริงที่ตอบไปแล้วภาษาหนึ่งจะไม่ย้อนไปเปลี่ยนสิ่งที่พูดไปแล้ว | ไม่จำเป็นเร่งด่วน — ทบทวนใหม่ตอน L1.5 ถ้าพบว่าพฤติกรรมจริงต้องต่าง |
| **D-5** | `PlanCardCompact`/`AdvisorRoom` ยังไม่มี unit test อัตโนมัติ (แค่ oracle grep + typecheck + ตาดูภาพ) เพราะเป็น UI ล้วน ไม่มี logic ซับซ้อนพอจะคุ้มเขียนเทส เก็บ logic ที่ซับซ้อนจริง (state machine) ไว้ที่ `adapter.ts` ซึ่งก็ยังไม่มีเทสอัตโนมัติเหมือนกัน (ไม่ใช่ oracle ที่ WO นี้บังคับ) | ถ้ามี WO ในอนาคตต้องการความมั่นใจสูงขึ้นกับ mock adapter ก่อนสลับเป็นของจริง |

---

## 8. จอที่ต้องเทียบภาพ (Fable — ทำตาม §0.4 ขั้น 5)

1. `?fixture=advisor-start` เทียบ `02-advisor-start.png` — ดู §6
2. `?fixture=advisor-plan` เทียบ `03-advisor-chat.png` — ดู §6
3. `/history` (ไม่มี mockup เฉพาะ — เทียบโครงกับ `.li` rows ของ `08-journal.png`)
4. `/plan` (ไม่มี mockup เฉพาะ — เป็นทางผ่านชั่วคราวตามที่ WO สั่งตรง ๆ "route stub page")
5. ทำซ้ำสลับภาษา EN ถ้าต้องการ (ยังไม่ได้ถ่ายภาพ EN ของห้องที่ปรึกษาเอง เพราะ WO ไม่ได้บังคับ parity ภาษาอังกฤษ — แค่บังคับว่าทุกสตริงต้องมีคู่ EN ใน i18n ซึ่งมีครบแล้ว ตรวจผ่าน fitness rule E)

**คำสั่งเรนเดอร์ที่ใช้ซ้ำได้**: `node /root/qc/l14-advisor-shots.js` (ดู §5 N-5 / §6) — สคริปต์นี้อยู่นอก repo (`/root/qc/`) เพราะเป็นเครื่องมือ QC ชั่วคราวใช้ครั้งเดียว ไม่ใช่ deliverable ของใบนี้ (ตาม `reference_bun_script_must_live_in_project` — ไม่ได้เขียนลง `/tmp` แต่ก็ไม่ใช่ของถาวรในโปรเจกต์นี้เหมือนกัน ถ้า Fable อยากเก็บถาวรค่อยย้ายเข้า `scripts/` ทีหลัง)

---

## 9. สิ่งที่ Fable ควรทำต่อ (§0.4 ขั้น 4–5)

1. รัน `./scripts/qc-L1.4.sh` ซ้ำ → ควรได้ 16/16
2. code review จุดที่ควรมองนานหน่อย: `adapter.ts` (state machine ทั้งไฟล์ — จุดที่ L1.5 จะมาแทนที่), `src/data/history.ts`/`history.web.ts` (แพตเทิร์นแยกไฟล์ตามแพลตฟอร์ม — ใบต่อไปที่แตะ `src/data` ต้องรู้เรื่องนี้ ดู D-2), `AdvisorRoom.tsx` (การจัดการ inverted list หลังแก้ N-4)
3. เปิดภาพ `.qc-shots/L1.4/parity-02.png` / `parity-03.png` เทียบเองอีกรอบตามกติกา §0.4 (ผมเทียบ+แก้ไปแล้ว 1 จุด (ชิปห่อแถว) แต่ Fable ควรยืนยันด้วยตาเองเผื่อมีจุดที่พลาดมองข้าม)
4. ตัดสิน N-1 (ตัดการเดาธีมจาก keyword ทิ้ง — เหลือแค่ default `'whale'` สำหรับข้อความอิสระ) ว่าเพียงพอสำหรับ WO นี้หรือไม่ ก่อนถึง L1.5
5. พิจารณา D-2 ว่าจะแก้ที่รากตอนนี้เลย (ปัญหา `expo-sqlite` เว็บ) หรือปล่อยเป็นแพตเทิร์น "แยกไฟล์ web ทุกจอที่แตะ `src/data`" ต่อไปเรื่อย ๆ
6. ยังไม่ต้องแจ้ง "พร้อม build" — L1.4 ไม่ใช่จุด build รอบไหน (ดู §4 ของ APP-RUN) ขั้นถัดไปคือ L1.5 (สมองที่ปรึกษาจริง) ที่จะมาแทน `adapter.ts`
