# ใบงาน L3.10 — R1 hotfix #2: ห้องที่ปรึกษา (ปุ่ม ▶ · สัญญาณ "กำลังคิด")

เจ้าของรายงาน 25 ก.ย. 2026 15:25 (TestFlight 0.1.0 (1) · iOS 27 · EN):
1. กด ▶ ที่แถว "Anchor cue" ในการ์ดแผน → **ไม่มีเสียง**
2. กดชิปธีม → AI ใช้เวลา ~4 s แต่**ไม่มีอะไรบอกว่ากำลังคิด** → เจ้าของกดซ้ำ 9 ครั้ง → ฟองผู้ใช้ 9 ฟอง + การ์ดแผน 2 ใบ + ปุ่ม Start 2 ปุ่ม

## สาเหตุ (Fable ไล่แล้ว)
- (1) `AdvisorRoom.tsx#MessageRow` เรนเดอร์ `<PlanCardCompact plan night testID>` **ไม่ส่ง `onPlayAnchor`** → ปุ่มไม่มี handler
- (2) `useAdvisor.ts` ไม่มีสถานะ busy · `pickChip` await ตรง ๆ · `rowLocked` อาศัย `chip.selected` ซึ่ง adapter ตั้งหลังคำตอบกลับ (หรือไม่ตั้งเลยสำหรับชิปธีม) → ระหว่างรอกดได้ไม่จำกัด

## งาน builder (แตะเฉพาะ src/advisor/* · src/ui/TypingBubble.tsx (ใหม่) · src/audio/player.ts (เพิ่มค่าคงที่/ฟังก์ชันเท่านั้น) · src/i18n · src/dev/fixtures.ts · scripts/visual-all.js บรรทัด ROUTES · **ห้ามแตะ** settings/devices/platform (L3.9 กำลังทำอยู่คู่ขนาน))
### A ปุ่ม ▶
- `src/audio/player.ts`: `export const PREVIEW_VOLUME = 0.5` + `export async function playAnchorPreview(signature, lang)` = `playAnchorOnce(signature, { volume: PREVIEW_VOLUME, pan: 0, lang })` (ได้ไฟล์เต็มระฆัง+กระซิบถ้ามี) · เหตุผล: 0.15 คือระดับเริ่มกลางคืนบนหูฟัง ฟังกลางวันผ่านลำโพงแทบไม่ได้ยิน
- `AdvisorRoom`: สร้าง signature จาก `getAnchorSeed()` + `buildAnchorSignature(seed, lang)` (แบบ `app/plan/index.tsx`) เก็บใน state · ส่ง `onPlayAnchor` ให้ `PlanCardCompact` · สถานะ `anchorBusy: 'idle'|'loading'|'playing'` → ปุ่มแสดง `ActivityIndicator` ตอน loading (ดาวน์โหลดไฟล์ครั้งแรก ~1–4 s) และไอคอน stop/สีจางตอน playing · `disabled` ระหว่าง loading/playing · จบแล้ว (~10 s หรือ promise resolve) กลับ idle
- ถ้าเล่นล้ม (ไม่มีหูฟัง/เสียงระบบ 0) ไม่ต้อง alert แต่ log

### B สัญญาณกำลังคิด + กันกดซ้อน
- `useAdvisor.ts`: `busyRef` + state `busy: boolean` · `say/pickChip/edit`: ถ้า busy → return ทันที (ละเว้น) · ตั้ง busy=true ก่อน await, false หลัง (finally) · เปิดเผย `busy` ใน `UseAdvisorResult`
- `adapter.ts#pickChip`: mark ชิปที่กดเป็น `selected: true` ในข้อความต้นทาง **ก่อน** เรียก AI (ล็อกแถวทันที) — ทั้ง engine adapter และ mock adapter
- `src/ui/TypingBubble.tsx`: ฟอง AI (สไตล์ `Bubble` role ai · night ได้) ข้างในจุด 3 จุด (6 px · gap 4) ขยับ opacity/translateY วน 1.2 s ด้วย `Animated` · `accessibilityLabel={t('advisor.thinking')}` ("กำลังคิด…"/"Thinking…")
- `AdvisorRoom`: เมื่อ `busy` → เรนเดอร์ `TypingBubble` ต่อท้ายรายการข้อความ (เลื่อนให้เห็น) · ชิปทุกแถว `onPress` = undefined ขณะ busy + tone จาง · ปุ่มส่ง/ไมค์ `disabled` ขณะ busy · Composer ยังพิมพ์ได้
- fixture `advisor-thinking` (`src/dev/fixtures.ts` + `useAdvisor`: mock adapter ที่ค้าง busy) + route `['advisor-thinking','/?fixture=advisor-thinking']` ใน `scripts/visual-all.js`

### C ส่งงาน
- `bash scripts/qc-L3.10.sh` เต็ม · typecheck · fitness · web export + `node scripts/visual-all.js` → ดูภาพ `advisor-thinking-th.png` และ `advisor-plan-th.png` เทียบ mockup `03-advisor-chat.png` (ฟองกำลังคิดต้องกลมกลืนสไตล์ Liquid Glass เดิม ไม่เพิ่มสี)
- note `ledger/wo-notes/L3.10.md` (ไทย · ตัวเลขจริง · ข้อต่าง · หนี้ · R1: กด ▶ ต้องได้ยินภายใน 4 s ครั้งแรก/ทันทีครั้งถัดไป · กดชิปรัว 5 ครั้ง = ฟองผู้ใช้ 1 ฟอง)
- ห้าม commit/push/eas · ห้ามเพิ่ม dependency · ห้ามแก้ scripts/qc-*
