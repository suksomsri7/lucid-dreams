# L1.3 — onboarding + ยินยอม + อุปกรณ์ 3 หมวด (builder: Sonnet) — โน้ตส่งงาน

วันที่ 24 ก.ย. 2569 · worktree `/root/projects/lucid-dreams-L1.3` · branch `wo/L1.3`
**ไม่ได้ commit มือ / ไม่ได้ push / ไม่ได้แตะ EAS** — autosave timer commit ให้เองทุก ~6 นาที (เห็นใน `git log`: `autosave … [wo/L1.3]`)

> **รอบ 2 (หลัง Fable parity review)** — 3 จุดที่แก้ตามที่ Fable สั่ง (ไม่ได้แก้ `scripts/qc-L1.3.sh` เอง — **Fable แก้ข้อสอบเองแล้ว** ยืนยันว่า regex O4.1 ที่รอบ 1 รายงานว่าเป็นบั๊ก (N-2 เดิม) ถูกแก้จริง):
> 1. **เอาสวิตช์ "ส่งข้อความฝันให้ AI" ออกจากจอต้อนรับ** — ไม่มีใน mockup ที่เจ้าของอนุมัติ (N-1 เดิม ยุติแล้ว: ตัดออก) `consentAi` ยังคง default `false` ในสโตร์เหมือนเดิม สวิตช์จริงจะไปอยู่ตั้งค่า (mockup 09 · L3.6)
> 2. **ปุ่มหลัก ("เริ่ม"/"พร้อมแล้ว") ปักไว้เหนือ home indicator ตลอด** — เพิ่ม prop `footer` ให้ `Screen.tsx` (เนื้อหาเลื่อนได้เหนือปุ่ม ปุ่มมี safe-area padding ของตัวเอง ไม่เลื่อนตาม) ใช้ทั้ง 2 จอ
> 3. **fixture QC เฉพาะเว็บ** `src/dev/fixtures.ts` (ใหม่) — `?fixture=devices` เติม Apple Watch Series 9 (84%) + Sleep A20 (92%) แบบต่อแล้วเข้า `DeviceRegistry` จริง · `?fixture=accepted` ติ๊ก checkbox ให้ตั้งแต่โหลดหน้า — คุมด้วย `__DEV__ || Platform.OS === 'web'` (ไม่มีทางเข้าไปอยู่ในแอป native release เลย)
>
> ระหว่างทางยังพบว่า **oracle ที่ Fable แก้แล้วบังคับให้คำ "treatment" ห้ามมี "medical" คั่นกลาง** (`grep -viE "...not treat"` เป็น substring match) — ปรับ EN "not medical care" → **"not treatment"** ให้ตรงคำแปลของ "ไม่ใช่การรักษา" พอดี (TH คืนกลับเป็นคำต้นฉบับของ mockup ได้เต็มที่แล้วตั้งแต่ Fable แก้ oracle) ผลคือ oracle **22/22** (ดู §0)

---

## 0. ผลข้อสอบ (รันเอง `./scripts/qc-L1.3.sh`)

```
ผล: ผ่าน 22 / 22
JSON_SUMMARY {"wo":"L1.3","pass":22,"fail":0,"failed":""}
```

รอบ 1 เคยได้ 21/22 (O4.1 ตกเพราะ oracle เองมีบั๊ก — regex `diagnos` จับคำว่า "Diagnostics" ผิด, ดูหลักฐานเดิมที่เคยอยู่ตรงนี้ใน git history ของไฟล์นี้) — **Fable แก้ `qc-L1.3.sh` เองแล้ว** (คอมมิทนอกใบนี้) จึงกลับไปคืนข้อความ TH ต้นฉบับของ mockup ("ไม่ใช่การรักษา") แล้ว oracle ผ่านครบ

งานอื่นที่รันผ่านแล้ว (ทุกงานหนักผ่าน `heavy.sh` ชื่อลงท้าย `-l13*`) — **รอบ 2** ตัวที่ขึ้นต้นด้วย `*b`/`*c`/`*d` คือรันซ้ำหลังแก้ 3 จุดที่ Fable สั่ง:

| คำสั่ง | ผล |
|---|---|
| `./scripts/heavy.sh prebuild-l13b "cd apps/mobile && npx expo prebuild -p ios --no-install"` | exit 0 · ยืนยัน `ios/LucidDream/{th,en}.lproj/InfoPlist.strings` ลงจริง (§ S10) · ลบ `ios/` ทิ้งแล้ว |
| `./scripts/heavy.sh typecheck-l13d "pnpm -s typecheck"` (รอบ 2 หลังแก้ 3 จุด) | exit 0 |
| `npx tsx scripts/fitness.mts` (รอบ 2 — ตกรอบแรกอีกครั้งเพราะคอมเมนต์ใหม่มีอักษรไทยหลุด แก้แล้ว) | ผ่านทั้ง 5 กฎ (A–E) · E นับได้ **141 คีย์** (ลด 2 จากรอบ 1 — ถอดคีย์ `onboarding.consent.aiLabel`/`.aiHint` ที่ไม่ใช้แล้วตามข้อ 1) |
| `./scripts/heavy.sh test-l13b "pnpm test"` | `@lucid/engine` **9/9 ผ่าน — ไม่ได้แตะไฟล์ทดสอบเดิมเลย** (ตามขอบเขต ห้ามเพิ่มไฟล์อื่นใน `packages/engine` นอกจาก `devices.ts` + export line) |
| `./scripts/heavy.sh export-l13b "cd apps/mobile && … expo export --platform web"` (รอบ 2) | exit 0 · `apps/mobile/dist/index.html` |

**เรนเดอร์จริง + เทียบ mockup ด้วยตา (รอบ 2 ใช้ fixture)** — ดู §4 (ตารางพาริตี)

---

## 1. ไฟล์

### ใหม่

| ไฟล์ | มี |
|---|---|
| `packages/engine/src/devices.ts` | `DeviceCategory` ('HEART'\|'AUDIO'\|'EYE') · `DeviceEntry` · `DeviceRegistry` (add/update/remove/list/byCategory/subscribe) · `summarizeDevices()` → found/connected/ok ต่อหมวด (HEART/AUDIO ต้อง ≥1 ต่อ, EYE ไม่บังคับเสมอ) — pure, ไม่ import react-native/expo |
| `apps/mobile/src/devices/registry.ts` | `deviceRegistry` (instance เดียวของแอป) + `refreshDevicesFromPlatform()` — อ่าน `watchSensorSource.getStatus()` (HEART) และ `audioPlayer.getStatus().route` (AUDIO) แบบ pure read ไม่ `start()`/`configureSession()` · ลงทะเบียน "มือถือบนที่นอน" เป็นอุปกรณ์ HEART ที่ `connected:false` เสมอ (ยังไม่มี BLE/accel จน L2.3) |
| `apps/mobile/src/store/onboarding.ts` | `hasOnboarded` · `consentSafety{accepted,policyVersion,acceptedAt}` (`POLICY_VERSION=1`) · `consentAi`(default false) — AsyncStorage-backed, `useOnboardingState()` hook, TODO(L1.8) mirror ไป `UserProfile` |
| `apps/mobile/app/onboarding/index.tsx` | จอ (ก) ต้อนรับ — โลโก้ 🌙 → ชื่อแอป → tagline → การ์ด 3 ข้อควรรู้ → checkbox รับทราบ → ปุ่ม "เริ่ม" ปักที่ `Screen`'s `footer` (disabled จนติ๊ก) · `Seg` ไทย/EN มุมบนขวา — **ไม่มีการ์ด AI switch แล้ว** (ถอดออกตามคำสั่ง Fable รอบ 2 · ดู §3 N-1) |
| `apps/mobile/app/onboarding/devices.tsx` | จอ (ข) อุปกรณ์ 3 หมวด — การ์ด HEART/AUDIO (แถวอุปกรณ์ที่ต่อจริง หรือ empty-state ถ้าไม่พบ + แถว "ค้นหาอุปกรณ์อื่น") · การ์ด EYE จาง (opacity .5) · การ์ด info เรื่องทดสอบเสียงย้ายไปก่อนนอน · ปุ่ม "พร้อมแล้ว" ปักที่ `footer` (ไม่ถูกบล็อกด้วยจำนวนอุปกรณ์ — ดู comment ในไฟล์) · เรียก `applyDeviceFoundFixture()` หลัง `refreshDevicesFromPlatform()` ทุกครั้งที่ mount · sheet "ค้นหาอุปกรณ์อื่น" (ของใหม่ ไม่มีใน mockup — placeholder ก่อน BLE L2.3) |
| `apps/mobile/locales/th/InfoPlist.strings` · `.../en/InfoPlist.strings` | คำขอสิทธิ์ 6 อย่าง (Health share/update · Microphone · Speech · Bluetooth · Notifications) ภาษาคน TH/EN |
| `apps/mobile/src/dev/fixtures.ts` | **ใหม่ (รอบ 2, Fable สั่ง)** — `applyDeviceFoundFixture()` (คีย์ `?fixture=devices` → เติม Apple Watch Series 9 ต่อแล้ว 84% + Sleep A20 ต่อแล้ว 92% เข้า `deviceRegistry` โดยใช้ id เดียวกับของจริง `WATCH_DEVICE_ID`/`HEADPHONES_DEVICE_ID` ให้ทับแทนที่ ไม่ใช่ซ้อนเป็นแถวที่ 2) + `isAcceptedFixtureRequested()` (`?fixture=accepted`) — ทุกฟังก์ชัน no-op ถ้าไม่ใช่ `__DEV__` และไม่ใช่เว็บ |

### แก้ไข

| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `packages/engine/src/index.ts` | +1 บล็อก export (`DeviceRegistry`, `summarizeDevices`, ชนิดที่เกี่ยว) — **บรรทัดเดียวตามขอบเขตที่ WO อนุญาต** ไม่แตะอย่างอื่นในแพ็กเกจนี้เลย |
| `apps/mobile/src/platform/types.ts` | `HealthImport.requestReadAccess` → **เปลี่ยนชื่อเป็น** `requestAuthorization` (WO สั่งตรง ๆ — เช็กก่อนว่าไม่มีจุดเรียกใช้จริงในโค้ดเลยตอนนั้น จึงเปลี่ยนชื่อได้โดยไม่พังอะไร ดู N-3) · เพิ่ม interface `NotificationsPermission{isAvailable,requestAuthorization}` + ฟิลด์ `notifications` ใน `PlatformBundle` |
| `apps/mobile/src/platform/index.ts` | export type `NotificationsPermission` เพิ่ม |
| `apps/mobile/src/platform/ios/IosPeripherals.ts` | เปลี่ยนชื่อเมธอดตาม types.ts · เพิ่ม `IosNotificationsPermission` (declared-but-not-wired แบบเดียวกับ `IosHealthImport`/`IosLiveStatus` เดิม — TODO L1.7/L3.1) |
| `apps/mobile/src/platform/native.ios.ts` | wire `IosNotificationsPermission` เข้า bundle |
| `apps/mobile/src/platform/android/index.ts` | เปลี่ยนชื่อเมธอด + เพิ่ม `AndroidNotificationsPermission` stub + wire |
| `apps/mobile/src/i18n/th.ts` / `en.ts` | รอบ 1: +43 คีย์ `onboarding.*` ต่อภาษา · รอบ 2: **-2 คีย์** (`onboarding.consent.aiLabel`/`.aiHint` ไม่มีจุดเรียกใช้แล้ว ลบทิ้ง) รวมสุทธิ **141 คีย์** · `onboarding.consent.medical.body` (TH) คืนกลับเป็นคำต้นฉบับของ mockup "เป็นการฝึกความฝัน ไม่ใช่การรักษา" (EN ปรับเป็น "This is dream training, not treatment." ให้ตรงคำแปล — ดูแบนเนอร์ด้านบน) |
| `apps/mobile/src/ui/Screen.tsx` | รอบ 1: เพิ่ม `contentStyle` (เลิกใช้แล้วในใบนี้หลังมี `footer`) · **รอบ 2**: เพิ่ม prop `footer?: ReactNode` — เรนเดอร์เป็น sibling ใต้ `ScrollView` (ที่ตอนนี้มี `style:{flex:1}` ชัดเจนแล้ว กันไม่ให้ footer แย่งพื้นที่จนพัง) มี `paddingBottom: insets.bottom + spacing.lg` ของตัวเอง — จอที่ไม่ส่ง `footer` (tabs/journal/settings/diagnostics) พฤติกรรมเดิมไม่เปลี่ยนเลย |
| `apps/mobile/app/_layout.tsx` | เพิ่มด่าน onboarding: `useOnboardingState()` + `useSegments()` — `!hydrated` → พื้นหลังเปล่า, `!hasOnboarded && !inOnboarding` → `<Redirect href="/onboarding"/>`, ไม่งั้นเรนเดอร์ `<Stack>` เดิม (เพิ่ม `<Stack.Screen name="onboarding"/>`) |
| `apps/mobile/app.config.ts` | เพิ่ม `CFBundleLocalizations:['en','th']` + plugin `withInfoPlistLocales` (inline ในไฟล์เดียวกัน — เหตุผลดู N-4) คัดลอก `locales/<lang>/InfoPlist.strings` เข้า `ios/<project>/<lang>.lproj/` ตอน prebuild |

---

## 2. Dependency ใหม่

**ไม่เพิ่มเลย** — ตรวจแล้ว `git diff --stat -- '*/package.json' pnpm-lock.yaml` ว่าง ทั้งใบ

- `@react-native-async-storage/async-storage` ใช้ของเดิมจาก L1.2 (เก็บ onboarding state คีย์ `lucid.onboarding.v1` แยกจาก `lucid.locale`)
- `@expo/config-plugins` (`withDangerousMod`) — **ไม่ได้เพิ่มใน package.json** เพราะมากับ `expo` เองอยู่แล้ว (ยืนยันด้วย `require.resolve` จากทั้ง root และ `apps/mobile` ก่อนใช้) — ตั้งใจไม่เพิ่มไลบรารีใหม่สำหรับงาน "คัดลอกไฟล์ตอน prebuild" ซึ่งใช้ของที่มีอยู่แล้วพอ
- ไม่เพิ่ม `expo-notifications` / HealthKit module ใด ๆ ตามที่ WO อนุญาตให้ stub ไว้ก่อน (ดู §5 หนี้)

---

## 3. ข้อแย้ง/หมายเหตุกับสัญญา WO (ไม่ได้แก้ข้อสอบ)

### N-1 — ✅ ยุติแล้ว (รอบ 2) — สวิตช์ "ส่งข้อความฝันให้ AI" บนจอต้อนรับ ไม่อยู่ใน mockup จริง
เปิด `ledger/design-app/01-onboarding.body.html` (mockup ต้นฉบับ) อ่านก่อนเขียน JSX ตามกติกา — การ์ด 3 ข้อควรรู้จบที่ "ข้อมูลอยู่ในเครื่อง" แล้วต่อด้วย checkbox รับทราบ กับปุ่ม "เริ่ม" เลย ไม่มีสวิตช์ AI ที่ไหนในหน้านี้ — รอบ 1 ใส่ตาม WO ที่สั่งตรง ๆ ไว้ก่อน แล้วส่งให้ Fable ตัดสิน (ตาม §0.4 ขั้น 5) **Fable ยืนยันแล้วว่า mockup ที่เจ้าของอนุมัติไม่มีสวิตช์นี้ → ถอดออก** `consentAi` ยังคง default `false` ในสโตร์เหมือนเดิม (ไม่กระทบ oracle O2.3/O2.4) สวิตช์จริงจะไปอยู่หน้าตั้งค่า (mockup 09 · WO L3.6) — ไฟล์ `apps/mobile/app/onboarding/index.tsx` ไม่มีการ์ดนี้แล้ว

### N-2 — ✅ ยุติแล้ว (รอบ 2) — 🔴 `qc-L1.3.sh`'s O4.1 มีบั๊ก: regex `diagnos` จับคำว่า "Diagnostics" (ชื่อจอเดิม) ผิด
ข้อสอบ:
```bash
bad=$(grep -rhoiE "cure|treat(ment)?|diagnos|รักษา|วินิจฉัย|บำบัด" $src/i18n/th.ts $src/i18n/en.ts 2>/dev/null | grep -viE "ไม่ใช่|not a|no medical|ไม่วินิจฉัย|ไม่รักษา|ไม่ใช่การรักษา" | wc -l)
```
`grep -o` ตัดเอาเฉพาะคำที่ match ทิ้งบริบทรอบข้าง — คีย์เดิมตั้งแต่ L1.1/L1.2 อย่าง `'diagnostics.title'`, `'diagnostics.section.device'` ฯลฯ (จอ "ตรวจระบบ" สำหรับทดสอบเครื่องจริง ไม่เกี่ยวการแพทย์เลย) มีคำว่า "Diagnostics" ซึ่งพอผ่าน `-o` เหลือแค่ `diagnos` (ตัด context "ไม่ใช่/not a" ทิ้งไปด้วย) เลยติดเป็น "bad" ทุกครั้ง — พิสูจน์แล้ว:
```
$ grep -rhoiE "cure|treat(ment)?|diagnos|รักษา|วินิจฉัย|บำบัด" apps/mobile/src/i18n/th.ts apps/mobile/src/i18n/en.ts | sort | uniq -c
      6 Diagnos
     74 diagnos
$ grep -n "diagnos" apps/mobile/src/i18n/th.ts apps/mobile/src/i18n/en.ts | grep -v "diagnostics\."
(ไม่มีผลลัพธ์ — ทุกจุดคือคีย์ `diagnostics.*` ล้วน)
$ grep -n "^  'onboarding\." apps/mobile/src/i18n/th.ts apps/mobile/src/i18n/en.ts | grep -iE "cure|treat|diagnos|รักษา|วินิจฉัย|บำบัด"
(ไม่มีผลลัพธ์ — คีย์ 43 อันที่ใบนี้เพิ่มไม่มีคำต้องห้ามเลยสักคำ)
```
**ยืนยันว่า 80/80 ที่นับได้มาจากคำว่า "Diagnostics" (ชื่อจอเดิม) ทั้งหมด ไม่มีจุดไหนเกี่ยวข้อความทางการแพทย์จริง และไม่มีจุดไหนที่ใบนี้เป็นคนเพิ่ม** — บั๊กนี้ต้องเคยแฝงอยู่ตั้งแต่ L1.1/L1.2 (คีย์ `diagnostics.*` มีมาก่อนแล้ว) แค่ยังไม่เคยมีใครรันเช็กนี้กับ i18n จริงมาก่อน (`O4.1` เป็นข้อสอบใหม่ที่เพิ่งเขียนสำหรับใบนี้)

**ผล**: Fable แก้ `qc-L1.3.sh`'s O4.1 เองแล้ว (เปลี่ยนเป็น word-boundary `\b(cure|cures|treat|treats|treatment|diagnose|diagnoses|diagnosis)\b|รักษา|วินิจฉัย|บำบัด` + เอา `grep -o` ออกให้ context ไม่หาย) — ยืนยันด้วยมือว่า "ไม่ใช่การรักษา" ผ่านสะอาดกับ regex ใหม่ (`bad=0`) **จึงคืนข้อความ TH กลับเป็นคำต้นฉบับของ mockup ทันที** (ดูแถวถัดไป) — oracle ผ่าน 22/22 (§0)

**ข้อความเรื่องการแพทย์ที่ใบนี้เขียนจริง** (3 ข้อในจอต้อนรับ) — รอบ 1 เคยเปลี่ยนคำ TH จาก "ไม่ใช่การรักษา" เป็น "ไม่ใช่การดูแลทางการแพทย์" เพื่อเลี่ยงบั๊กข้างบน **รอบ 2 คืนกลับเป็นคำต้นฉบับแล้ว** (oracle ใหม่รองรับ): TH = "เป็นการฝึกความฝัน ไม่ใช่การรักษา" (ตรง mockup 100%) · EN = "This is dream training, not treatment." (เดิมใช้ "not medical care" แล้วปรับเป็น "not treatment" อีกรอบ เพราะ exclude-list ใหม่ของ oracle เช็ก substring "not treat" ตรง ๆ ไม่รู้จัก "not medical treatment" ที่มีคำคั่นกลาง — ตรวจแล้วว่า "not treatment" ผ่านสะอาด)

### N-3 — เปลี่ยนชื่อ `HealthImport.requestReadAccess` → `requestAuthorization`
WO สั่งตรง ๆ ให้เพิ่มเมธอดชื่อนี้ — เดิมมี `requestReadAccess()` ทำหน้าที่เดียวกันอยู่แล้ว (เพิ่งสร้างใน L1.1 spike ยังไม่มีใครเรียกใช้จริงเลย — เช็กด้วย `grep -rn "requestReadAccess"` ก่อนแก้ พบแค่ 3 จุดประกาศ/implement ไม่มีจุดเรียกใช้) แทนที่จะมี 2 เมธอดซ้ำซ้อน (`requestReadAccess` เดิม + `requestAuthorization` ใหม่ทำงานเหมือนกัน) เลือก **เปลี่ยนชื่อเดิมทั้งระบบ** ให้เหลือชื่อเดียว ตรงกับ `SpeechToText.requestPermissions()` / `NotificationsPermission.requestAuthorization()` ใหม่ที่ตั้งชื่อคู่กัน — ปลอดภัยเพราะไม่มี call site เดิมพังจากการเปลี่ยนชื่อ

### N-4 — plugin `withInfoPlistLocales` ต้องเขียนสดในไฟล์ `app.config.ts` ไม่ใช่ไฟล์แยก
ลองแยกเป็น `apps/mobile/plugins/withInfoPlistLocales.ts` ก่อน (ตามที่ WO ใบ้ไว้ "a small config plugin") แต่รัน `expo prebuild` แล้วล้มด้วย `Cannot find module './plugins/withInfoPlistLocales'` — `expo/config`'s loader ทรานส์ไพล์เฉพาะไฟล์ `app.config.ts` เองแบบ on-the-fly แต่ไม่ได้ส่ง require ของไฟล์ TS ที่ import แบบ relative ผ่าน loader เดียวกันต่อ (ไฟล์ `.ts` ธรรมดาเรียกด้วย `require()` ตรง ๆ ไม่ผ่าน transpile) — ย้ายโค้ดทั้งหมดเข้าไปอยู่ใน `app.config.ts` ไฟล์เดียวแล้วรัน prebuild ซ้ำ ผ่าน (ดู §0 · หลักฐานไฟล์ `.lproj` ที่ได้จริง)

**ขอบเขตที่ตั้งใจไม่ทำ**: plugin คัดลอกไฟล์ `.strings` เข้า `ios/<project>/<lang>.lproj/` เท่านั้น **ไม่ได้ลงทะเบียนเป็น `PBXVariantGroup` ใน `.pbxproj`** (ขั้นที่ทำให้ Xcode build จริงเอาไฟล์นี้ไปรวมในแอป) — เครื่องนี้ไม่มี Mac/Xcode ให้ทดสอบการแก้ pbxproj ทำผิดมีสิทธิ์ได้ project file พังแบบเงียบ ๆ (โผล่ตอนมีคน build จริงครั้งแรก) ซึ่งแย่กว่าการทิ้งหนี้ที่รู้ตัวไว้ชัดเจน — บันทึกเป็นหนี้ §5

### N-5 — คำอธิบายใบ L1.3 ใน `APP-RUN.md` §2 บรรทัดเดียวเก่ากว่ามติ mockup
`APP-RUN.md` §2's L1.3: "...ทดสอบเสียง ▶ + slider 15%" — เทียบกับ `DESIGN-APP.md` §4-01 (ตัวมockup จริง) กับ §3.2 ขั้น 3 ที่มีวงเล็บชัดเจนว่า "(การทดสอบเสียงย้ายไปทำก่อนเริ่มทุกคืน)" (มติ 24 ก.ย. รอบ 10/13 — ย้ายไปเป็นจอแยกก่อนนอนทุกคืนแทน เป็นงานของ L1.6) — `qc-L1.3.sh` เองก็ไม่มีข้อสอบเรื่องทดสอบเสียง/slider เลยสักข้อ สนับสนุนว่าบรรทัดสรุปใน §2 เป็นของเก่าที่ยังไม่อัปเดตตามมติภายหลัง ไม่ใช่คำสั่งจริง — **ยึดตาม mockup + WO deliverables list (ไม่มีอ้างถึงหูซ้าย/ขวา/slider เลย) เป็นหลัก ไม่ได้สร้าง UI ทดสอบเสียงในใบนี้**

### N-6 — `scripts/visual.sh ... th` เรนเดอร์เป็นอังกฤษ (ไม่ใช่บั๊กของแอป — chromium บนเครื่องนี้ไม่รับ `--lang`)
รัน `visual.sh ... th` ตามที่สั่ง ได้ภาพเป็น**ภาษาอังกฤษ**ทั้งที่ควรเป็นไทย — ตรวจแล้วพบว่า snap chromium headless บนเครื่องนี้เพิกเฉยต่อ `--lang=th` จริง ๆ:
```
$ chromium --headless --lang=th --dump-dom "data:text/html,<script>document.write(new Intl.DateTimeFormat().resolvedOptions().locale)</script>"
→ en-US
```
แอปตรวจภาษาเริ่มต้นจาก `Intl.DateTimeFormat().resolvedOptions().locale` (ตาม `src/i18n/index.ts` ที่ L1.2 วางไว้ อ่านจาก `navigator.language`) — เมื่อ chromium ไม่ยอมเปลี่ยนค่านี้ แอปก็เดาเป็น `en` ถูกต้องตามตรรกะของมันเอง (ไม่ใช่บั๊ก แค่เครื่องมือถ่ายภาพใช้ไม่ได้กับกลไกนี้) — ตรงกับที่ L1.2's builder เจอมาก่อนแล้วและแก้ด้วยการ "คลิกจริงผ่าน DOM" แทน (`ledger/wo-notes/L1.2.md` §0) ผมทำแบบเดียวกัน: เขียนสคริปต์ `puppeteer-core` ชั่วคราว (ใช้ `/root/qc/node_modules/puppeteer-core` ที่มีอยู่แล้วในเครื่อง ไม่เพิ่ม dependency ในโปรเจกต์) คลิกปุ่ม `Seg` "ไทย" จริงที่ `/onboarding` แล้วถ่ายภาพ 2 จอ (locale ติดไปกับ `localStorage` ตอน goto หน้าอุปกรณ์ต่อ) — สคริปต์อยู่ที่ `/tmp/l13-th-shots.js` ไม่ commit เข้า repo (ไม่ใช่ deliverable เหมือนที่ L1.2 ทำ) — ภาพ TH ใน `.qc-shots/L1.3/` ที่ใช้ทำพาริตี §4 คือภาพจากสคริปต์นี้ ไม่ใช่จาก `visual.sh` ตรง ๆ

**รอบ 2**: ทำสคริปต์เดียวกันซ้ำ (`/tmp/l13-th-shots2.js`) เพิ่มแค่ query string `?fixture=accepted`/`?fixture=devices` ต่อท้าย URL ก่อน goto ตามที่ Fable สั่ง — ผลลัพธ์คือภาพ TH ที่ตรง mockup แทบทุกพิกเซล (ดู §4 ตารางใหม่)

### N-7 — ทำไมใส่ fixture ที่ id เดียวกับของจริง ไม่ใช่ entry แยก
`applyDeviceFoundFixture()` เรียก `deviceRegistry.add({id: WATCH_DEVICE_ID, ...})` ทับ id เดิมที่ `refreshDevicesFromPlatform()` เพิ่งเขียนไป (ไม่ใช้ id ใหม่เช่น `fixture-apple-watch`) — เพราะ `DeviceRegistry` เก็บเป็น `Map` คีย์ด้วย `id` การ `add()` ซ้ำ id เดิมคือ "แทนที่" ไม่ใช่ "เพิ่มแถวใหม่" (ตรงกับ contract `add()`'s docstring ใน `packages/engine/src/devices.ts` — "Insert or fully replace") ผลคือมีอุปกรณ์ HEART หนึ่งแถวเป๊ะ ไม่ใช่ 2 แถวซ้อนกันแบบที่ตัวหนึ่งถูกกรองออกเงียบ ๆ · เป็นผลพลอยได้ที่ดีคือ `deviceSubtitle()` ที่เขียนไว้ตั้งแต่รอบ 1 (เช็ก `entry.id === WATCH_DEVICE_ID` เพื่อต่อท้าย "ใส่นอนได้ทั้งคืน") ใช้ได้กับข้อมูล fixture ทันทีโดยไม่ต้องแก้อะไรเพิ่ม — subtitle ที่ได้ ("ต่อแล้ว · ใส่นอนได้ทั้งคืน · แบต 84%") ตรง mockup ตัวอักษรเป๊ะโดยบังเอิญ (ไม่ได้ตั้งใจ hardcode ให้ตรง แค่ผลจากใช้ id เดียวกัน)

---

## 4. ตารางพาริตี — รอบ 2 (เทียบภาพคู่ MOCKUP | RENDER, ใช้ fixture)

ภาพหลัก (ตามที่ Fable สั่งเจาะจง — TH + fixture): `/root/projects/lucid-dreams/.qc-shots/L1.3/parity-01a-th.png` (จอ ก, `?fixture=accepted`) · `parity-01b-th.png` (จอ ข, `?fixture=devices`) — เปิดดูจริงแล้ว (390×844, ครอปจาก mockup เฟรม 0/1) — ภาพ EN (`parity-01a-en.png`/`parity-01b-en.png`, **ไม่ใช้ fixture** แค่ตรวจสอบเองเพิ่มว่าไม่พังหลังแก้ 3 จุด ไม่ใช่สิ่งที่ Fable สั่งให้ทำรอบนี้) ก็สร้างไว้ด้วยแล้วเหมือนกัน

### จอ ก — ต้อนรับ (`onboarding/index.tsx`, `?fixture=accepted`)

| องค์ประกอบ | mockup | ของจริง | ตัดสิน |
|---|---|---|---|
| โครง/ลำดับ (โลโก้→ชื่อ→tagline→การ์ดข้อควรรู้→checkbox→ปุ่ม) | ตามนี้ | ตรงเป๊ะ **ไม่มีการ์ด AI แทรกแล้ว** | ✅ ตรง |
| `Seg` ไทย/EN มุมบนขวา | มี | มี ทำงานจริง | ✅ ตรง |
| ข้อความข้อควรรู้ทั้ง 3 ("ไม่ใช่เครื่องมือแพทย์ · ไม่ใช่การรักษา", "ไม่แนะนำสำหรับบางคน", "ข้อมูลอยู่ในเครื่อง") | ตัวอักษรต้นฉบับ | **ตรงตัวอักษร 100% ทั้ง 3 ข้อ** (คืนคำ "ไม่ใช่การรักษา" กลับตามต้นฉบับแล้ว — oracle รองรับแล้วหลัง Fable แก้) | ✅ ตรง |
| ไอคอนหัวใจ/i/โล่ (heart/info/shield) | ตามนี้ | ตรง (พอร์ตจาก `_base.part` เดิมของ L1.2) | ✅ ตรง |
| checkbox "รับทราบ" | ติ๊กแล้ว (การ์ดดำ+เครื่องหมายถูก) | **ติ๊กแล้วเหมือนกัน** (`?fixture=accepted`) | ✅ ตรง |
| ปุ่ม "เริ่ม" | ดำทึบ พร้อมกด | **ดำทึบ พร้อมกดเหมือนกัน** | ✅ ตรง |
| ปุ่มปักตำแหน่งเหนือ home indicator | ปักไว้ล่างสุดของเฟรม | **ปักไว้ล่างสุดจริง** ผ่าน `Screen`'s `footer` prop ใหม่ (สแกนภาพแล้ว ระยะห่างจากขอบล่างใกล้เคียงมาก) | ✅ ตรง |
| สวิตช์ "ส่งข้อความฝันให้ AI" | ไม่มีเลยใน mockup | **ไม่มีแล้ว** (ถอดออกตามคำสั่ง Fable) | ✅ ตรง — N-1 ยุติแล้ว |
| สถานะแถบเวลา/แบต/ปุ่ม home indicator วาดเป็นภาพ | มี (chrome ของ mockup) | ไม่มี (เป็น chrome ของอุปกรณ์จริง ไม่ใช่ของแอป) | ✅ ยอมรับปกติ — ทุกจอเป็นแบบนี้เหมือนกันหมด ไม่ใช่เรื่องเฉพาะใบนี้ |
| กระจกจริง (Liquid Glass) เทียบ blur fallback | เบลอแบบมี glow ชัด | เบลอจางกว่าเล็กน้อย | ✅ ยอมรับปกติ — `expo-blur` fallback บนเว็บ ของจริงบน iOS 26 ใช้ `expo-glass-effect` (หนี้ QC ทั่วทั้งระบบดีไซน์ ไม่ใช่ของใบนี้) |

### จอ ข — อุปกรณ์ (`onboarding/devices.tsx`, `?fixture=devices`)

| องค์ประกอบ | mockup | ของจริง | ตัดสิน |
|---|---|---|---|
| หัว "อุปกรณ์" + "ขั้นที่ 2 จาก 2" + subtitle | ตามนี้ | ตรงตัวอักษร 100% | ✅ ตรง |
| หมวด 💓 ชีพจร/การขยับ — Apple Watch Series 9 | "ต่อแล้ว · ใส่นอนได้ทั้งคืน · แบต 84%" จุดเขียว | **ตรงตัวอักษร 100% + จุดเขียวมี glow เหมือนกัน** (จาก `applyDeviceFoundFixture()`) | ✅ ตรง |
| หมวด 💓 — "ค้นหาอุปกรณ์อื่น" + คำอธิบาย | ตามนี้ | ตรงตัวอักษร 100% | ✅ ตรง |
| หมวด 🎧 เสียง — Sleep A20 | "ต่อแล้ว · หูฟังนอน · แบต 92%" จุดเขียว | **ตรงตัวอักษร 100%** | ✅ ตรง |
| หมวด 👁 การเคลื่อนไหวดวงตา — จาง + ข้อความ | ตามนี้ | ตรงตัวอักษร 100% + จางเหมือนกัน (opacity .5) | ✅ ตรง |
| การ์ด info "การทดสอบเสียงย้ายไปทำก่อนเริ่มทุกคืน..." | ตามนี้ | ตรงตัวอักษร 100% | ✅ ตรง |
| ปุ่ม "พร้อมแล้ว" ปักตำแหน่งเหนือ home indicator | ดำทึบ พร้อมกด ปักล่างสุด | ดำทึบ พร้อมกด ปักล่างสุดจริง (`footer` prop) | ✅ ตรง |
| sheet "ค้นหาอุปกรณ์อื่น" (กดแล้วเปิด) | ไม่มีใน mockup เลย | มี (การ์ดลอยพร้อมชิป "เร็ว ๆ นี้") | **ยอมรับ+เหตุผล** — WO ขอไว้ตรง ๆ ป้องกันไม่ให้แถว "+" เป็นทางตันก่อน BLE (L2.3) จะมาจริง — ไม่กระทบภาพหลักเพราะเป็น Modal ที่กดถึงจะเห็น |

**สรุปรอบ 2**: ทั้งสองจอตรง mockup แทบทุกองค์ประกอบ (โครง/ลำดับ/ข้อความ/ไอคอน/สี/ตำแหน่งปุ่ม) จุดต่างที่เหลือมีแค่ 1 จุดคือ sheet ค้นหาอุปกรณ์ (ของใหม่ WO ขอเพิ่ม ไม่ใช่ปุ่มที่กดโดยไม่ตั้งใจ) — ไม่มีจุดไหนเหลือ "ยังไม่ตัดสิน" แล้ว

ตรวจ EN ด้วย (ไม่ใช้ fixture) — โครง/ตำแหน่งปุ่มเหมือนกันทุกอย่าง ไม่มีสตริงล้นกรอบ ไม่พัง

---

## 5. หนี้ที่ทิ้งไว้

| # | หนี้ | ใบที่ควรจ่าย |
|---|---|---|
| **D-1** | `withInfoPlistLocales` คัดลอกไฟล์ `.strings` เข้า `ios/*/​*.lproj/` จริง แต่ยังไม่ลงทะเบียนใน `.pbxproj` (`PBXVariantGroup`) — Xcode build จริงจะยังไม่หยิบไฟล์นี้ไปใช้จนกว่าจะมีคนต่อสาย native (ดู N-4) | R1 (build รอบแรกบนเครื่องจริง) หรือ L3.6 (เจ้าของ Store compliance ที่เหลือ เช่น `PrivacyInfo.xcprivacy`) |
| **D-2** | มือถือบนที่นอน (`phone-on-mattress`) ลงทะเบียนใน `DeviceRegistry` เป็น `connected:false` เสมอ ยังไม่มีทางเชื่อมต่อจริง (ต้อง `expo-sensors` accelerometer) | L2.3 |
| **D-3** | `NotificationsPermission`/`HealthImport.requestAuthorization` เป็น stub คืน `false` เสมอ (ไม่มี native module จริง ตามที่ WO อนุญาตให้ stub) | L1.7/L3.1 (notifications) · L2.9 (HealthKit) |
| **D-4** | sheet "ค้นหาอุปกรณ์อื่น" เป็นแค่รายการชิป "เร็ว ๆ นี้" ยังกดอะไรไม่ได้จริง (ไม่มี BLE scan) | L2.3 |
| **D-5** | ~~สวิตช์ "ส่งข้อความฝันให้ AI"~~ — **ปิดแล้ว (รอบ 2)**: Fable ตัดสินให้ตัดออกจากจอนี้ ย้ายไปตั้งค่า | L3.6 (ตั้งค่า) |
| **D-6** | ~~`O4.1` regex บั๊ก~~ — **ปิดแล้ว (รอบ 2)**: Fable แก้ `qc-L1.3.sh` เองแล้ว oracle 22/22 | — |
| **D-7** | `src/dev/fixtures.ts` เป็นโค้ดจริงที่ build เข้าไปในบันเดิลเว็บ/dev เสมอ (แค่ตัวมันเองไม่ทำอะไรถ้าไม่ใช่ `__DEV__`/web) — ยังไม่มีเทสต์อัตโนมัติยืนยันว่ามันจะไม่มีทางถูกเรียกบน native release (พึ่ง `Platform.OS`/`__DEV__` เท่านั้น ไม่มี build-time strip) | ถ้ากังวลเรื่องขนาดบันเดิล/ความเสี่ยง ให้พิจารณา babel plugin ตัด dead code ตอน build release ใน WO ที่ทำ native build จริง (R1) |

---

## 6. เช็กลิสต์ความปลอดภัย §0.5 (ส่วนที่ใบนี้เกี่ยว)

| # | ผล |
|---|---|
| **S6 ความปลอดภัยผู้ใช้ (health)** | ✅ ข้อความยินยอมครบ 3 ข้อ + checkbox บังคับ + บันทึกเวอร์ชันนโยบาย/เวลา · ไม่มีคำอ้างทางการแพทย์ในข้อความจริง (ยืนยันด้วย oracle ที่แก้แล้ว 22/22) |
| **S9 supply chain** | ✅ ไม่มี dependency ใหม่เลย (§2) |
| **S10 Store compliance** | 🟡 บางส่วน — ข้อความขอสิทธิ์ TH/EN ครบ 6 อย่าง + คัดลอกเข้า `ios/*.lproj/` จริง (ยืนยันด้วย prebuild) แต่ยังไม่ผูกเข้า `.pbxproj` (D-1) · `PrivacyInfo.xcprivacy` ยังไม่ทำ (นอกขอบเขตใบนี้ ของ L3.6) |
| อื่น ๆ (S1–S5,S7,S8) | ไม่เกี่ยวกับใบนี้ (ไม่มีเซิร์ฟเวอร์/เสียง/เซนเซอร์จริงที่ใบนี้แตะ) |

---

## 7. สิ่งที่ Fable ควรทำต่อ (§0.4 ขั้น 4–5)

**สถานะหลังรอบ 2**: ทั้ง 3 ข้อที่สั่งทำครบแล้ว oracle 22/22 · N-1/N-2 ยุติแล้ว (ดู §3) · ไม่มีจุด "ยังไม่ตัดสิน" เหลือในตารางพาริตี §4

1. รัน `./scripts/qc-L1.3.sh` ซ้ำ → ควรได้ 22/22
2. เปิดภาพพาริตี 4 ใบใน `.qc-shots/L1.3/parity-01{a,b}-{th,en}.png` เทียบเองอีกรอบ (ผมสรุปตาราง §4 ไว้แล้วแต่ Fable ต้องยืนยันตามกติกา §0.4 ขั้น 5) — เน้นภาพ `-th.png` 2 ใบที่ใช้ fixture เพราะเทียบ happy-path ตรงกับ mockup ได้ตรงที่สุด
3. code review จุดที่ควรมองนานหน่อย (รอบ 2 เพิ่ม): `src/ui/Screen.tsx` (prop `footer` ใหม่ + `ScrollView`'s `style:{flex:1}` ที่เพิ่มเข้ามาพร้อมกัน), `src/dev/fixtures.ts` (การ์ดว่าไม่มีทางรั่วเข้า native release จริงไหม — ดู D-7), `app.config.ts` (plugin function + cast `as unknown as string` ที่ §3 N-4 อธิบายเหตุผล), `app/_layout.tsx` (ตรรกะ redirect ผ่าน `useSegments`), `src/platform/types.ts` (เปลี่ยนชื่อเมธอดที่ N-3)
4. ยังไม่ต้องแจ้ง "พร้อม build" — L1.3 ไม่ใช่จุด build รอบไหน (ดู §4 ของ APP-RUN)
