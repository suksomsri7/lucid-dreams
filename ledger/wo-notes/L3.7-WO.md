# ใบงาน L3.7 — แบรนด์: ไอคอน M · หน้าเปิดแอป (intro) มีเอฟเฟกต์ · เสียงสมอ 1 รอบ

เจ้าของสั่ง 25 ก.ย. 2026 (TG หลัง msg 3522):
1. ใช้ไอคอน **M** (`ledger/design-icon/icon-M-zzz-glass-light.png`) — Fable เตรียม asset ให้แล้ว (ดู §A)
2. ตอนเปิดแอปให้แสดงโลโก้นี้ **พร้อมเอฟเฟกต์**
3. เมื่อโหลดเสร็จ **ก่อนเข้าหน้าหลัก** ให้เล่นเสียง(สมอ) 1 รอบ
4. เมื่อกด **Start tonight** ให้เล่นเสียงอีก 1 รอบ

ภาพแบบ: `ledger/design-app/11-splash.png` (กรอบ 0 = โลโก้นิ่ง · กรอบ 1 = วงแสงสว่างสุดตอนเสียงเล่น) · ข้อสอบ `scripts/qc-L3.7.sh` (27 ข้อ) · parity ต้องผ่านตา Fable ก่อน merge (APP-RUN §0.2 rule 9)

## A. ของที่ Fable ทำไว้แล้ว (อยู่ใน main แล้ว ห้ามสร้างซ้ำ)
- `apps/mobile/assets/icon.png` 1024² RGB (ไอคอน M เต็ม) · `assets/splash-icon.png` (เฉพาะตัว Zzz บนโปร่งใส 1024²) · `assets/adaptive-icon.png` (foreground Android อยู่ใน safe zone 66%)
- `targets/watch/Assets.xcassets/AppIcon.appiconset/{icon-1024.png,Contents.json}`
- SVG ต้นทาง `ledger/design-icon/logo-M-glyphs.svg` — path 3 ตัว (viewBox 1024): `M208 198 H528 L208 518 H528` (w50) · `M578 388 H818 L578 628 H818` (w44) · `M300 662 H500 L300 852 H500` (w38) · เส้นไล่สี #ffffff→#f0eeff บน-ล่าง · เงา dy18 blur18 #4b3fb8 α.32 · ไฮไลต์ = path เดิมเลื่อนขึ้น 7 หน่วย stroke ขาว α.7 w14/12/10

## B. งานของ builder
### B1 ไอคอน (app.config.ts)
- `icon: './assets/icon.png'` · `android.adaptiveIcon: { foregroundImage: './assets/adaptive-icon.png', backgroundColor: '#f4f2ff' }` (Android เฟส 2 แต่โค้ดต้องรองรับ — rule 8)
- watch: ตรวจว่า `@bacons/apple-targets` type 'watch' หยิบ `Assets.xcassets/AppIcon.appiconset` ที่วางไว้ให้เอง (อ่านซอร์สปลั๊กอินใน node_modules · ถ้าต้องระบุ `icon:` ใน `targets/watch/expo-target.config.js` ให้ระบุเป็นไฟล์เดียวกัน) — บันทึกผลที่พบใน note

### B2 native splash → intro ไร้รอยต่อ
- เพิ่ม `expo-splash-screen` (เวอร์ชันตาม SDK 57 `npx expo install`) · plugin ใน app.config: `['expo-splash-screen', { image: './assets/splash-icon.png', imageWidth: 200, resizeMode: 'contain', backgroundColor: '#f6f5fb' }]` (สีเดียวกับ `backgroundColor` ของแอป)
- `app/_layout.tsx`: `SplashScreen.preventAutoHideAsync()` ที่ module scope · ซ่อน (`hideAsync`) ทันทีที่ Intro overlay วาดเฟรมแรก (onLayout) → โลโก้ native กับโลโก้ JS ต้องอยู่ตำแหน่ง/ขนาดเดียวกัน (กลางจอ กว้าง 200) เพื่อไม่ให้กระโดด

### B3 Intro overlay `src/intro/`
- `IntroOverlay.tsx` (react-native-svg วาด Zzz ด้วย path ใน §A + LinearGradient + เงา (วาดสำเนา path สีเงา blur ไม่ได้ใน svg RN → ใช้ path สำเนาสี #4b3fb8 α.18 เลื่อนลง 12 หน่วย 2 ชั้น) + ไฮไลต์) · wordmark ใต้โลโก้จาก i18n `intro.wordmark` = "DREAMING" ทั้ง th/en (letterSpacing 2, สี ink2, 15) — ตามภาพ 11
- เอฟเฟกต์ (Animated ของ RN หรือ reanimated ที่มีอยู่แล้ว ห้ามเพิ่ม dep): โลโก้จางเข้า 0→1 + scale .92→1 ใน 600 ms easing out · วงแสง (halo) วงกลม 260 ไล่สีม่วง α.35 หายใจ scale 1→1.15 / α .35→.6 วนลูป 2.4 s ease-in-out · ตอนเสียงเล่น (B4) ให้ halo ขยายเป็น 300 α .7 ชั่วครู่ (กรอบ 1 ของภาพ 11)
- จังหวะ: `INTRO_MIN_MS = 900` (เห็นเอฟเฟกต์อย่างน้อยเท่านี้) · เมื่อ `hydrated` (onboarding store) และครบ MIN → เรียก `playBrandAnchor()` (ไม่ await) → รออีก 1400 ms → overlay จางออก 400 ms → หน้าหลัก/onboarding ที่อยู่ข้างใต้โผล่ · `INTRO_MAX_MS = 6000` เพดานรอ hydrate ถ้าช้าให้ไปต่อโดยไม่รอ · เสียงเล่นต่อจนจบเองหลัง overlay หาย (ไม่ค้างจอ 10 วิ — มติ Fable: "ก่อนเข้าหน้าหลัก" = เสียง**เริ่ม**ก่อนหน้าหลักโผล่)
- แสดงครั้งเดียวต่อการเปิดแอป (ตัวแปร module-level `playedThisLaunch`) · ไม่ขึ้นซ้ำเมื่อ navigate/re-render · ครอบทั้ง onboarding และ tabs (วางใน `_layout.tsx` เป็น overlay absolute ทับ Stack ไม่ใช่ route)
- เว็บ (QC export): ขึ้นเฉพาะ `?fixture=intro` และค้างสถานะ "โลโก้เต็ม + halo peak" ไม่จางออก (ให้ถ่ายภาพได้) · จออื่นห้ามมี overlay (`Platform.OS === 'web'` guard) · เพิ่ม fixture `intro` ใน `src/dev/fixtures.ts` และ route `['intro','/?fixture=intro']` ใน `scripts/visual-all.js` (ไฟล์นี้ Fable อนุญาตให้แตะเฉพาะบรรทัด ROUTES)
- ห้ามมีข้อความไทยในโค้ด (fitness rule B) · ไม่มีปุ่ม ไม่มีข้อความอื่นตามภาพ 11

### B4 เสียงสมอ 1 รอบ `src/audio/brand.ts`
- `export async function playBrandAnchor(): Promise<void>` = `getAnchorSeed()` → `buildAnchorSignature(seed, lang)` (lang จาก i18n locale ปัจจุบัน แบบเดียวกับ `app/plan/index.tsx`) → `playAnchorOnce(sig, { volume, pan: 0 })` · `volume` = ค่าที่ผู้ใช้ตั้งใน settings store (L3.6 มี volume) ถ้าไม่มี/ยังไม่ hydrate ใช้ `0.15` (ค่าเดียวกับปุ่ม ▶ ฟังในแผน) · try/catch กลืน error แล้ว log (เสียงแบรนด์ห้ามทำแอปพัง)
- intro เรียกตอนโหลดเสร็จ (B3) · `src/advisor/AdvisorRoom.tsx` `handleStart`: เรียก `void playBrandAnchor()` **ก่อน** `router.push('/plan')` โดยไม่ await
- บนเว็บ `playAnchorOnce` stub อยู่แล้ว ไม่ต้องทำอะไรเพิ่ม

## C. ส่งงาน
- รัน: `pnpm --filter mobile typecheck` (หรือ `cd apps/mobile && pnpm typecheck`) · `pnpm fitness` · `cd apps/mobile && npx expo export --platform web` · `bash scripts/qc-L3.7.sh` ต้อง 27/27 · ถ่ายภาพ `node scripts/visual-all.js` (ดูวิธีใช้หัวไฟล์) แล้วทำ parity: `scripts/parity.sh ledger/design-app/11-splash.png 1 <shot intro-th.png> /tmp/claude-0/parity-11.png` และดูเองก่อนส่ง
- เขียน `ledger/wo-notes/L3.7.md`: สิ่งที่ทำ · ผลทุกคำสั่ง (ตัวเลขจริง) · ข้อที่ต่างจากใบงานและเหตุผล · หนี้ · จุดที่ต้องตรวจบนเครื่องจริง R1 (เสียงตอนเปิดแอปเล่นจริงไหมเมื่อสวิตช์เงียบ/หูฟัง BT)
- ห้าม: commit/push · `eas` ทุกคำสั่ง · แก้ `scripts/qc-*.sh` · แก้ oracle/mockup · เพิ่ม dependency นอกจาก `expo-splash-screen` · แตะ `apps/api` · แตะ engine
