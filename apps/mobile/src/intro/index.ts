/** WO L3.7 — the launch screen. `app/_layout.tsx` only ever needs `IntroOverlay`. */

export { INTRO_MAX_MS, INTRO_MIN_MS, IntroOverlay } from './IntroOverlay';
export { BrandMark, HALO_SIZE, LOGO_SIZE } from './BrandMark';
