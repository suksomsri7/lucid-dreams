/** iOS resolution of the glass wrapper → real Liquid Glass (with its own blur fallback). */

export { GlassSurface, hasRealGlass } from './ios/GlassSurface';
export type { GlassSurfaceProps } from './ios/GlassSurface';
