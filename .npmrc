# Metro/React Native ยังตามลิงก์สัญลักษณ์ของ pnpm ไม่ครบ → ใช้ node_modules แบบ hoisted
# (แนวทางที่ Expo แนะนำสำหรับ monorepo ที่ใช้ pnpm)
node-linker=hoisted
# เปิดให้ expo/react-native รัน postinstall ที่จำเป็น (patch-package / expo-modules)
enable-pre-post-scripts=true
